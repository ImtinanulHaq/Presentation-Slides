services:
  - type: web
    name: presentation-frontend
    runtime: node
    buildCommand: "npm install && node create-favicon.js && npm run build"
    startCommand: "node server.js"
    envVars:
      - key: REACT_APP_API_URL
        value: "https://presentation-slides.onrender.com/api"
      - key: REACT_APP_API_TOKEN
        value: "presentation_api_token_production_2026"
      - key: NODE_ENV
        value: "production"
      - key: CI
        value: "false"
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: PORT
        value: "3000"
